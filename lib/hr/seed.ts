import type { EmployeeProfile } from "./types";

/**
 * Demo seed removed: payroll/contracts must never ship in the client bundle.
 * HR data is loaded from Supabase under leadership RLS only.
 */
export const HR_SEED: EmployeeProfile[] = [];
