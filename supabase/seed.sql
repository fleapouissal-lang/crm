-- Fusion Leap CRM — Seed Data (référence SQL)
-- Préférez la commande automatique :
--   npm run db:seed
--   npm run db:seed -- --force   (réinitialise la démo)

-- Compte démo créé par le seed :
--   Email    : demo@fusionleap.ma
--   Password : Demo123!
--   Org      : Fusion Leap Demo

-- Le script TypeScript (lib/db/seed.ts) crée via service_role :
--   1 organisation, 1 admin, 7 leads, 6 tasks, 5 activities
